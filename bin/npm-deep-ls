#!/usr/bin/env node
const deepLs = require('../src');

if (process.argv.length < 3) {
  console.error('Please provide a package name as an argument');
  process.exit(1);
}

const packageName = process.argv[2];

deepLs(packageName);
